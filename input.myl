enum Test where 
    A(Number), B();
end

fun main() : Void do
    val first := "Hello ";
    val second := " world";
    match (first, second) do 
        case ("Hello ", world) := do print "hello "+world; end;
        case (some, " world") := do print "some "+some+" world"; end;
        case (hello , world) := do print "other: "+hello+world; end;
    end;
end

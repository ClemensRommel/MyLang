fun fibbonacciN(n) := {
    var last := 1;
    var lastBefore := 1;
    for i in [1..n - 2] do {
        val lastBefore2 := lastBefore;
        lastBefore := last;
        last := last + lastBefore2;
    };
    last
};

/* druckt /* die fibbonacci */ zahlen */
fun main() := {
    print(fibbonacciN(46));
};